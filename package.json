{
  "name": "owen-exporter",
  "version": "0.1.15",
  "description": "Obsidian plugin for exporting SVG embeds and Markdown selections.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "test": "node tests/run-tests.mjs",
    "build": "npm run test && tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "release": "node release.mjs"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "export",
    "svg",
    "html"
  ],
  "author": "Owen Lee",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.10",
    "esbuild": "^0.23.0",
    "obsidian": "^1.6.7",
    "tslib": "^2.6.3",
    "typescript": "^5.5.3"
  }
}
